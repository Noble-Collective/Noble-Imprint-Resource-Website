node_modules/
.env
service-account-key.json
*.log
.DS_Store
SESSION.md
nc-logo.png
mockups/
# Firebase
.firebase/

# Content tree cache — committed so a freshly deployed container ships a warm
# snapshot and serves it instantly (stale-while-revalidate) instead of blocking
# the first request on a full GitHub rebuild. Refreshed nightly by refresh-cache.yml.

# Test
test-results/
test-screenshots/

# Local scratch / dev artifacts
temp/
cookies.txt
.claude/
